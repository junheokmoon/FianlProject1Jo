<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.withy.mapper.UserMapper">
	<select id="selectUserJoindateList" resultType="UserDTO">
		<![CDATA[
		select * from (select user_no, user_id, user_password, user_name, user_email, user_phone
			, user_status, user_birthday, user_nickname, user_joindate from usertable 
			order by user_joindate desc) where rownum <= 5
		]]>
	</select>
	
	<select id="selectUserList" resultType="UserDTO">
		select user_no, user_id, user_password, user_name, user_email, user_phone
			, user_status, user_birthday, user_nickname, user_joindate from usertable order by user_no desc
	</select>
			
	<select id="selectUserinfo" resultType="UserDTO">
		select user_no, user_id, user_password, user_name, user_email, user_phone
			, user_status, user_birthday, user_nickname, user_joindate from usertable where user_id=#{user_id}
	</select>
</mapper>