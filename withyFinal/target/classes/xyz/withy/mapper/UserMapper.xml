<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.withy.mapper.userMapper">
	<select id="selectUserId" resultType="UserDTO"><!-- 회원아이디로 조회하기 -->
		select user_no, user_id, user_password, user_name, user_email, user_phone, user_status
			, user_birthday, user_nickname, user_joindate from usertable where user_id=#{user_id}
	</select>
	
	<select id="selectUserNoList" resultType="UserDTO"><!-- 회원번호 내림차순 정렬 -->
		select user_no, user_id, user_password, user_name, user_email, user_phone
			, user_status, user_birthday, user_nickname, user_joindate from usertable order by user_no desc
	</select>

	<select id="selectUserJoindateList" resultType="UserDTO"><!-- 회원가입일 최신순으로 정렬 -->
		select user_no, user_id, user_password, user_name, user_email, user_phone
			, user_status, user_birthday, user_nickname, user_joindate from usertable order by user_joindate desc
	</select>
</mapper>