<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.withy.mapper.InquiryMapper">
	<insert id="insertInquiry">
		<selectKey resultType="int" keyProperty="inquiryNo" order="BEFORE">
			select inquiry_seq.nextval from dual
		</selectKey>
		insert into inquiry values(#{inquiryNo}, #{inquiryTitle}, #{inquiryContent}, sysdate, null, null, 1, 1, 
			#{inquiryImage}, #{inquiry_user_no})			
	</insert>
	
	<!-- 이미지파일 변경하는 경우와 미변경 경우 구분해서 동적쿼리 -->
	<update id="updateInquiry">
		update notice 
		<set>
			<if test="inquiryImage != null and inquiryImage != ''">
				inquiry_title=#{inquiryTitle}, inquiry_content=#{inquiryContent}, inquiry_image=#{inquiryImage}
				,  
			</if>
			<if test="noticeImage == null and noticeImage == ''">
				notice_title=#{noticeTitle}, notice_content=#{noticeContent}, notice_update=sysdate  
			</if>
		</set>
		 where inquiry_no=#{inquiryNo}		
	</update>
	
	<!-- 조회수 증가  -->	
	<update id="updateNoticeCount">
		update notice set notice_count=#{noticeCount+1} where notice_no=#{noticeNo}
	</update>
	
	<!-- 글 삭제 -->
	<delete id="deleteNotice">
		delete from notice where notice_no=#{noticeNo} <!-- te -->
	</delete>
	
	<!-- 글번호 전달받아 단일행 검색 -->
	<select id="selectNotice" resultType="NoticeDTO">
		select notice_no, notice_title, notice_content, notice_image, notice_date, notice_update, notice_count
			from notice where notice_no=#{noticeNo}
	</select>
	
	<!-- 전체 게시글 갯수 -->
	<select id="selectNoticeCount" resultType="int">
		select count(*) from notice
	</select>
	
	<!-- 페이징 처리 -->
	<select id="selectNoticeList" resultType="NoticeDTO">
		select * from (select rownum rn, notice.* from (select notice_no, notice_title
			, notice_content, notice_image, notice_date, notice_update, notice_count, notice_user_no
			from notice order by notice_no desc) notice) where rn between #{startRow} and #{endRow}
	</select>
	
	<!-- 검색대상과 검색단어를 전달받아 공지사항 테이블에 저장된 게시글 중 
	검색 대상의 컬럼에 검색단어가 포함된 게시글의 갯수를 검색하는 쿼리
	=> 검색 기능 사용하지 않을 경우 공지사항 테이블에 저장된 모든 게시글 갯수 검색하여 출력 -->
	
</mapper>